(function(){
'use strict';
function replaceOnce(text,oldValue,newValue,label){const i=text.indexOf(oldValue);if(i<0)throw new Error('Patch introuvable: '+label);return text.slice(0,i)+newValue+text.slice(i+oldValue.length);}
window.applyReproductionV1=function(html){
html=replaceOnce(html,"  <div class=\"panel\" id=\"title\">Ferme avicole<span>Élevage · tapis à œufs · emballage</span></div>\n  <div class=\"panel\" id=\"stats\">Poules actives : <b id=\"hensCount\">6</b><br>Œufs traités : <b id=\"eggCount\">0</b><br>Poussins nés : <b id=\"chickCount\">0</b></div>\n  <div class=\"panel\" id=\"hint\">Glissez pour tourner · molette pour zoomer · touchez un animal pour l'entendre</div>","  <div class=\"panel\" id=\"title\">Ferme avicole<span>Élevage · reproduction · couvaison</span></div>\n  <div class=\"panel\" id=\"stats\">Poules : <b id=\"hensCount\">6</b><br>Œufs pondus : <b id=\"eggCount\">0</b><br>Œufs fécondés : <b id=\"fertileCount\">0</b><br>Poussins nés : <b id=\"chickCount\">0</b></div>\n  <div class=\"panel\" id=\"hint\">Glissez pour tourner · pincez ou utilisez la molette pour zoomer · touchez une poule ou le coq</div>","ui");
html=replaceOnce(html,"  // Packaging shed (traitement)\n  const shed = building(13,-6, 6.5,5.5,3.6, COLORS.trim, 0x6B7280, 2.1);\n\n  // crates near shed\n  for(let i=0;i<5;i++){\n    const crate = new THREE.Mesh(new THREE.BoxGeometry(0.9,0.6,0.9), mat(0xE7D9B8));\n    crate.position.set(15.5 + (i%3)*1.05, 0.3 + Math.floor(i/3)*0.62, -8.5);\n    crate.castShadow=true;\n    const edges = new THREE.LineSegments(new THREE.EdgesGeometry(crate.geometry), new THREE.LineBasicMaterial({color:0x8B5A2B}));\n    crate.add(edges);\n    scene.add(crate);\n  }\n\n  // ---------- CONVEYOR BELT ----------\n  const beltY = 1.1;\n  const beltStartX = -11, beltEndX = 9.5, beltZ = -6;\n  const beltLen = beltEndX-beltStartX;\n  const beltMetalTex = metalTexture();\n  beltMetalTex.repeat.set(10,2);\n  const belt = new THREE.Mesh(new THREE.BoxGeometry(beltLen,0.25,1.4), new THREE.MeshStandardMaterial({map:beltMetalTex, roughness:0.55, metalness:0.55}));\n  belt.position.set((beltStartX+beltEndX)/2, beltY, beltZ);\n  belt.castShadow = true; belt.receiveShadow = true;\n  scene.add(belt);\n  for(let i=0;i<9;i++){\n    const leg = new THREE.Mesh(new THREE.BoxGeometry(0.18,beltY,0.18), mat(COLORS.woodDark));\n    leg.position.set(beltStartX + i*(beltLen/8), beltY/2, beltZ-0.55);\n    scene.add(leg);\n    const leg2 = leg.clone(); leg2.position.z = beltZ+0.55;\n    scene.add(leg2);\n  }\n  // quality-check glow midpoint\n  const glowLight = new THREE.PointLight(0xFFD98E, 1.2, 4);\n  glowLight.position.set((beltStartX+beltEndX)/2, beltY+1.2, beltZ);\n  scene.add(glowLight);\n\n  // eggs on belt\n  const eggGeo = new THREE.SphereGeometry(0.16,10,8);\n  eggGeo.scale(1,1.3,1);\n  const eggs = [];\n  function spawnEgg(startPos){\n    const e = new THREE.Mesh(eggGeo, mat(COLORS.egg,0.4,0));\n    e.castShadow = true;\n    e.position.copy(startPos);\n    scene.add(e);\n    eggs.push({mesh:e, phase:'toBelt', t:0, start:startPos.clone()});\n  }\n  let eggCount = 0;\n  const eggCountEl = document.getElementById('eggCount');\n\n  const receiveCrate = new THREE.Mesh(new THREE.BoxGeometry(1.1,0.5,0.9), mat(0xE7D9B8));\n  receiveCrate.position.set(beltEndX+0.75, 0.28, beltZ);\n  receiveCrate.castShadow = true; receiveCrate.receiveShadow = true;\n  scene.add(receiveCrate);\n  receiveCrate.add(new THREE.LineSegments(new THREE.EdgesGeometry(receiveCrate.geometry), new THREE.LineBasicMaterial({color:0x8B5A2B})));\n\n","  // ---------- ŒUFS ET REPRODUCTION ----------\n  // La chaîne industrielle est volontairement retirée pour cette première étape :\n  // on se concentre sur l'élevage, la fécondation, la couvaison et les poussins.\n  const eggGeo = new THREE.SphereGeometry(0.16,16,12);\n  eggGeo.scale(1,1.28,1);\n  const nestEggs = [];\n  let eggCount = 0;\n  const eggCountEl = document.getElementById('eggCount');\n  const fertileCountEl = document.getElementById('fertileCount');\n\n  function eggAtNest(nest){\n    return nestEggs.find(e=>e.nest===nest) || null;\n  }\n\n  function updateEggCounters(){\n    eggCountEl.textContent = eggCount;\n    fertileCountEl.textContent = nestEggs.filter(e=>e.fertilized).length;\n  }\n\n  function createNestEgg(nest, fertilized, hen){\n    if(eggAtNest(nest)) return null;\n    const shell = fertilized ? 0xFFF0CF : COLORS.egg;\n    const mesh = new THREE.Mesh(eggGeo, smoothMat(shell,0.38,0));\n    mesh.castShadow = true;\n    mesh.position.set(nest.x,0.42,nest.z);\n    mesh.rotation.z = (Math.random()-0.5)*0.15;\n    scene.add(mesh);\n    const egg = {mesh,nest,fertilized,hen,age:0,incubator:null