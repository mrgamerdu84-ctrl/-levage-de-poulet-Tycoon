oNest';\n            c.target.set(c.nest.x,0,c.nest.z);\n          }\n        }\n      }\n      else if(c.state==='toHen'){\n        const hen=c.courtshipTarget;\n        if(!hen || hen.state==='nesting' || hen.state==='brooding'){\n          c.state='wander';c.courtshipCooldown=3;\n        }else{\n          const dir=new THREE.Vector3(hen.group.position.x-c.group.position.x,0,hen.group.position.z-c.group.position.z);\n          const dist=dir.length();\n          if(dist>0.75){\n            dir.normalize();c.group.position.x+=dir.x*1.45*dt;c.group.position.z+=dir.z*1.45*dt;\n            c.group.rotation.y=Math.atan2(dir.x,dir.z);\n            c.legs[0].rotation.x=Math.sin(t*10)*0.5;c.legs[1].rotation.x=-Math.sin(t*10)*0.5;\n          }else{\n            fertilizeHen(hen);\n            c.state='wander';c.courtshipTarget=null;c.courtshipCooldown=12+Math.random()*10;\n            playCluck();\n          }\n        }\n      }\n      else if(c.state==='toNest' || c.state==='leaving'){\n        const dest=c.state==='toNest'?c.target:new THREE.Vector3(c.home[0],0,c.home[1]);\n        const dir=new THREE.Vector3(dest.x-c.group.position.x,0,dest.z-c.group.position.z);\n        const dist=dir.length();\n        if(dist>0.15){\n          dir.normalize();c.group.rotation.y=Math.atan2(dir.x,dir.z);\n          c.group.position.x+=dir.x*1.3*dt;c.group.position.z+=dir.z*1.3*dt;\n          c.legs[0].rotation.x=Math.sin(t*10+c.phase)*0.5;c.legs[1].rotation.x=-Math.sin(t*10+c.phase)*0.5;\n          c.group.position.y=Math.abs(Math.sin(t*10+c.phase))*0.03;\n        }else if(c.state==='toNest'){\n          if(eggAtNest(c.nest)){c.state='leaving';}\n          else{c.state='nesting';c.stateTimer=1.8+Math.random()*0.8;}\n        }else{\n          c.state='wander';c.stateTimer=5+Math.random()*7;\n        }\n      }\n      else if(c.state==='nesting'){\n        c.group.scale.y=c.baseScale*0.72;c.group.position.y=-0.05*c.baseScale;c.head.rotation.x=0.15;\n        if(c.stateTimer<=0){\n          const fertilized=c.fertileEggs>0;\n          const egg=createNestEgg(c.nest,fertilized,c);\n          if(fertilized)c.fertileEggs--;\n          playCluck();\n          c.layTimer=16+Math.random()*14;\n          if(egg && fertilized && c.broody){\n            egg.incubator=c;c.currentEgg=egg;c.state='brooding';c.stateTimer=14+Math.random()*5;\n          }else{\n            c.fed=false;c.hydrated=false;c.state='leaving';\n          }\n        }\n      }\n      else if(c.state==='brooding'){\n        c.group.scale.y=c.baseScale*0.68;c.group.position.y=-0.07*c.baseScale;\n        c.head.rotation.x=0.08+Math.sin(t*1.2+c.phase)*0.06;\n        if(!c.currentEgg || !nestEggs.includes(c.currentEgg)){\n          c.currentEgg=null;c.fed=false;c.hydrated=false;c.state='leaving';\n        }else if(c.stateTimer<=0){\n          hatchEgg(c.currentEgg);c.currentEgg=null;c.fed=false;c.hydrated=false;c.state='leaving';\n        }\n      }\n    });\n\n    // Les œufs non couvés restent visibles quelques instants, puis un fermier les ramasse.\n    for(let i=nestEggs.length-1;i>=0;i--){\n      const egg=nestEggs[i];\n      egg.age+=dt;\n      if(!egg.incubator){\n        egg.mesh.rotation.y+=dt*0.15;\n        if(egg.age>24) removeNestEgg(egg);\n      }\n    }","statemachine");
html=replaceOnce(html,"    // eggs: roll off the nest onto the belt, ride it, then drop into the crate\n    for(let i=eggs.length-1;i>=0;i--){\n      const eg = eggs[i];\n      if(eg.phase==='toBelt'){\n        eg.t += dt*1.8;\n        const p = Math.min(1, eg.t);\n        const dest = new THREE.Vector3(beltStartX, beltY+0.28, beltZ);\n        eg.mesh.position.lerpVectors(eg.start, dest, p);\n        eg.mesh.position.y += Math.sin(p*Math.PI)*0.4;\n        eg.mesh.rotation.z += dt*3;\n        if(p>=1) eg.phase = 'onBelt';\n      }\n      else if(eg.phase==='onBelt'){\n        eg.mesh.position.x += dt*1.4;\n        eg.mesh.rotation.z += dt*1.4;\n        if(eg.mesh.position.x >= beltEndX){\n          eg.phase = 'dropping'; eg.t = 0; eg.start = eg.mesh.position.clone();\n        }\n      }\n      else {\n        eg.t += dt*2.2;\n        const p = Math.min(1, eg.t);\n        eg.mesh.position.x = eg.start.x + (receiveCrate.position.x-eg.start.x)*p;\n        eg.mesh.position.y = eg.start.y + Math.sin(p*Math.PI)*0.5 - p(eg.start.y-0.35);\n        eg.mesh.rotation.z += dt*4;\n        if(p>=1){\n          scene.remove(eg.mesh);\n          eggs.splice(i,1);\n          eggCount++;\n          eggCountEl.textContent = eggCount;\n        }\n      }\n    }\n    glowLight.intensity = 1.0 + Math.sin(t*4)*0.3;\n\n","","oldconveyorloop");
return html;
};
})();
